Review all design docs in /docs for implementation readiness. Exclude docs inside /docs/done/.

## Instructions

For each doc: 

1) Create a testable checklist with pass/fail criteria, 
2) Identify any gaps or ambiguities, 
3) Cross-reference against our deployment requirements and implementation consistency. Use sub-tasks to parallelize across docs, then consolidate into a single markdown report with prioritized action items, which should be provided in each doc to make it better. Don't stop until every checklist item is specific enough to verify in under 5 minutes.
4) Verify the order correctness and propose changes if needed to implement all of docs in better order/